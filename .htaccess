# ─────────────────────────────────────────────────────────────────────────────
# go2trek.com Redirect Engine — Apache .htaccess
#
# This file tells Apache: "For every incoming URL, run index.php".
# index.php then reads redirect_map.csv and fires the correct 301.
#
# HOW TO TEST IF THIS IS WORKING:
#   After uploading, visit any old go2trek.com URL in your browser.
#   You should land on fetnepal.com. If you get a "file not found" or
#   blank page instead, your host is likely using Nginx — see README.txt.
# ─────────────────────────────────────────────────────────────────────────────

Options -Indexes

RewriteEngine On

# If the request is for a real file that exists on disk (e.g. robots.txt,
# favicon.ico), serve it directly — do not pass it to index.php.
RewriteCond %{REQUEST_FILENAME} !-f

# For everything else, send the request to index.php.
# The [L] flag means "stop processing rules after this one".
# The [QSA] flag means "append query string" so ?lang=en etc. is preserved.
RewriteRule ^ index.php [L,QSA]
